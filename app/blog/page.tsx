import { Header, Footer } from '../components';
import { blogPosts, site } from '../data';
export const metadata = { title: 'Virtual assistant hiring blog', description: 'Simple guides for virtual assistant costs, tasks, onboarding, and provider comparison.' };
export default function Blog(){ return <><Header/><main className="section"><div className="container"><p className="eyebrow">Blog</p><h1>Plain guides for hiring assistant help</h1><p className="lead">Use these guides to compare costs, tasks, providers, and onboarding steps before you hire.</p><div className="cards">{blogPosts.map((p)=><a className="card" href={`/blog/${p.slug}`} key={p.slug}><span className="pill">{p.minutes} min read</span><h3>{p.title}</h3><p>{p.excerpt}</p></a>)}</div><div className="card" style={{marginTop:24}}><h2>How to use these guides</h2><p>Start with pricing, then pick the first tasks, then compare VA support with an employee. Use the onboarding checklist once you have a shortlist.</p><p><a className="btn secondary" href="/provider-vetting">Open provider vetting checklist</a></p></div></div></main><Footer/></> }
