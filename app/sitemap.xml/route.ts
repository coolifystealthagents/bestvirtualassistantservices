import * as data from '../data'; import {fleetServices,postsPerPage} from '../fleet-data'; import {allBlogPosts,allResearchPosts} from '../content-library'; import {alternatives} from '../alternatives/data'; import {guides} from '../non-competitor-alternatives/data';
export function GET(){const d=data as any;const base=`https://${d.site.domain.toLowerCase()}`;const blogs=allBlogPosts;const pages=Math.max(1,Math.ceil(blogs.length/postsPerPage));const paths=['','/services','/blog','/blog/top-50-virtual-assistant-companies','/research','/alternatives','/non-competitor-alternatives','/contact-us','/privacy','/terms','/cancellation-policy',...fleetServices.map(s=>`/services/${s.slug}`),...blogs.map(b=>`/blog/${b.slug}`),...Array.from({length:Math.max(0,pages-1)},(_,i)=>`/blog/page/${i+2}`),...allResearchPosts.map(r=>`/research/${r.slug}`),...alternatives.map(a=>`/alternatives/${a.slug}`),...guides.map(g=>`/non-competitor-alternatives/${g.slug}`)];const body=paths.map(path=>`<url><loc>${base}${path}</loc></url>`).join('');return new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${body}</urlset>`,{headers:{'content-type':'application/xml'}})}
